/**
 * Global billing switch.
 *
 * All FitExtremes account types (Social User, Personal Trainer, Business Owner)
 * are currently FREE. Payment/subscription code is intentionally kept in the
 * codebase so billing can be re-enabled later by flipping this flag to `true`.
 */
export const BILLING_ENABLED = false;
